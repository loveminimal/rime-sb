# Rime schema
# encoding: utf-8

__include: sbfd.schema.yaml:/

schema:
  schema_id: sbmd
  name: "声笔魔单"
  icon: "assets/fly_o.ico"
  description: |
    —— 隐藏了候选框的飞单, 强制盲打_ᵢₙg...

__patch:
  # schema/icon: "assets/fly_y.ico"
  style/+:
      preedit_type: preview
      # inline_preedit: false
      layout:
        __include: style.yaml:/layout_h
        margin_y: -1
        # min_width: 0