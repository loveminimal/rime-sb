嘿，那个神奇的「 [声笔输入法](https://github.com/sbsrf/sbsrf) 」~

魔改了哪些内容呢？它：

```
基本功能跟随源仓库，增加额外定制功能，如下：
- 新增魔法飞单方案 sbfd_m.schema.yaml
- 新增声笔英文方案 sben.schema.yaml
- 增加快符插件 quick_symbol.lua
- 自定义文件修改为 patch.txt
- 引入新的主题 style.yaml
- 添加 AHK 支持
- ……
```

魔法飞单方案是什么？它：

```
基本功能跟随声笔飞单，添加并击支持，如下：
- 并击组合直接输出大写字母「 空明码指法 」
- 小写字母提供并击组合：
  - we → q  sd → a  xc → z  io → p  
  - kl → ;  er → ！ ui → ？ cv → 「  m, →  」
  - sf | jl → '
  
  - df | jk → 空格
  - fg | hj → 缩进
  - xv → ……  m. → ——
```

魔法飞单方案为什么而来呢？

```
- 为了减轻小手指的负担
- 为了方便键入大写字母（临时调用飞码）
- 同时兼容原声笔飞单的输入方式
- ……
```